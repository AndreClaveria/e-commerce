{% extends 'base.html.twig' %}

{% block body %}

	<h1 class="text-center">Mon panier</h1>
	{% if items %}
		<table class="table">
			<thead>
				<tr>
					<th>Product</th>
					<th>Prix</th>
					<th>Quantité</th>
					<th>Total</th>
					<th></th>
				</tr>
			</thead>
			<tbody>
				{% for item in items %}
					<tr>
						<td>{{ item.product.nom }}</td>
						<td>{{ item.product.prix }}</td>
						<td>
						{{ item.quantity }}
							<a href="{{ path('cart_add', {id: item.product.id}) }}" class="btn-sm btn-danger">
								<i>+1</i>
							</a>
							<a href="{{ path('cart_sub', {id: item.product.id}) }}" class="btn-sm btn-danger">
								<i>-1</i>
							</a>
							
						</td>
						<td>{{ item.product.prix * item.quantity }}€</td>
						<td>
							<a href="{{ path('cart_remove', {id: item.product.id}) }}" class="btn-sm btn-danger">
								<i class="fas fa-trash"></i>
							</a>
						</td>
					</tr>
				{% endfor %}
			</tbody>
            <tfoot>
            
            <tr>
                <td colspan="3" class="text-end">Total :</td>
                <td>{{ total }}€    </td>
            </tr>

            </tfoot>
		</table>
	{% else %}
		<p>Mon panier est vide !</p>
	{% endif %}
{% endblock %}
